# solver-policies

Synthetic fixture for Composer's pool policies (security advisories, the malware filter list, abandoned packages). The registry in `fixtures/registry/solver-policies.tar.gz` is hand-made (packages under `acme/` do not exist); nothing is downloaded from it, only resolved.
