[package]
name = "vivacity-resolver"
description = "Port of Composer's dependency resolver — semver, Packagist metadata, pool, solver, lock file — for vivacity"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
readme = "README.md"
keywords = ["composer", "php", "resolver", "semver"]
categories = ["development-tools", "command-line-utilities"]

[dependencies]
vivacity-core = { path = "../vivacity-core", version = "0.7.0" }
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
pcre2.workspace = true
tempfile.workspace = true

[dev-dependencies]
tempfile.workspace = true
