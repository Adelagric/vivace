# v0.5 — `require`, `remove`, mises à jour partielles

## 1. Cadrage

**Problème.** Depuis 0.4.0, `vivace update` résout tout `composer.json` et
écrit le même lock que Composer. Il manque les trois commandes qui font
évoluer un projet au quotidien : `composer update vendor/name` (partielle),
`composer require`, `composer remove`. Toutes trois reposent sur la même
brique : une mise à jour **partielle**, où les paquets non listés restent
verrouillés à la version du lock.

**Contrainte.** Même promesse que 0.4 : `composer.json` et `composer.lock`
identiques à l'octet à ce que Composer 2.10.3 écrit. Pour `require`, cela
inclut l'édition de `composer.json` **sans en changer la mise en forme**
(Composer édite le texte avec `JsonManipulator`, pas en ré-encodant), et le
choix de la contrainte `^x.y` quand l'utilisateur n'en donne pas.

**Non-objectifs.** `--dry-run`, les questions interactives (Composer
n'interroge pas en `--no-interaction`, c'est le mode de vivace), `--with`
(contraintes temporaires ; la plomberie existe dans `RepositorySet`, la
commande viendra après), `--lock` / `--minimal-changes` /
`update-with-minimal-changes` / `--patch-only` / `--root-reqs`, l'audit,
la recherche de paquets similaires (`levenshtein` sur l'API search), les
messages de `Problem` (R5, toujours reportés : un ensemble insoluble
échoue avec le bon code de retour et un message court). **Refusés
explicitement** quand ils changeraient les octets : `bump-after-update`
(config ou option — Composer lance `bump` après l'update et réécrit
`composer.json` ; port à part, plus tard), `update-with-minimal-changes`.

**Filtre de sécurité du pool.** Composer 2.10.3 retire par défaut du pool
toute version non-dev visée par un avis de sécurité (`policy` vrai,
`SecurityAdvisoryPoolFilter`, sauf paquets verrouillés), désactivable par
`--no-blocking` / `COMPOSER_NO_BLOCKING=1`. vivace ne le porte pas encore :
il se comporte comme `COMPOSER_NO_BLOCKING=1`. La capture des instantanés
tourne avec cette variable pour que live == rejeu, HANDOVER le dit, et le
port (avis capturés dans l'instantané via `security-advisories.api-url`)
est le jalon P7, avant 0.5.0.

**Critères de succès observables.**
1. Oracle « étapes » : pour des fixtures avec un état de départ et une liste
   de commandes (`update x`, `update x -w`, `require x`, `require x:^2`,
   `require --dev x`, `remove x`), `composer.json` et `composer.lock` après
   chaque étape sont identiques à l'octet entre Composer et vivace, sur un
   instantané Packagist figé. Six scénarios au moins, en CI.
2. Oracle du pool en mise à jour partielle : `tools/oracle-pool.php
   --update a/b [--with-dependencies]` — pool, décisions, opérations et lock
   identiques (la machinerie `skippedLoad`/`unlock` de `PoolBuilder`,
   portée en R1 sans oracle, est exercée ici).
3. Oracle `JsonManipulator` : un corpus de `composer.json` réels et
   synthétiques (indentation à 2/4 espaces et tabulations, clés absentes,
   sections vides, tri `sort-packages`, commentaires impossibles mais
   valeurs exotiques) passé par le phar et par le port, sortie identique.
4. Oracle `VersionSelector` : pour chaque paquet des instantanés, la
   contrainte recommandée (`findRecommendedRequireVersion`) et le candidat
   choisi (`findBestCandidate` avec la plateforme locale) identiques.

## 2. Ce que fait Composer (lu dans les sources vendorées)

- **Partielle** (`UpdateCommand` → `Installer::setUpdateAllowList`,
  `setUpdateAllowTransitiveDependencies`) : liste en minuscules dédoublonnée
  ; sans lock → erreur « Cannot update only a partial set of packages
  without a lock file present » ; `PoolBuilder` verrouille les paquets du
  lock hors liste (`lockPackage`), les débloque de proche en proche selon
  le mode (`UPDATE_ONLY_LISTED` / `…NO_ROOT_REQUIRE` / `…WITH_TRANSITIVE_DEPS`),
  avertit sur les motifs sans correspondance. `Installer::setUpdateAllowList`
  refuse `--with` et les motifs vides.
- **`require`** (`RequireCommand`) : `parseNameVersionPairs` (paire
  `nom[:= ]contrainte`, ou deux arguments quand le second n'a pas de `/`
  et n'est pas un nom de plateforme), avertissement sur `1.2` trop strict ;
  la casse des noms est conservée telle que tapée. Sans contrainte,
  `findBestVersionAndNameForPackage` tourne **toujours** (validation :
  paquet introuvable, plateforme incompatible, stabilité ; et le nom
  devient `getPrettyName()`) ; ensuite, avec `--no-update`, la contrainte
  calculée (`findRecommendedRequireVersion`) est écrite tout de suite ;
  sinon (`--fixed` compris) c'est `guess` : on requiert `*`, **une seule
  résolution** par le solveur, puis `updateRequirementsAfterResolution`
  prend le paquet verrouillé (`getPrettyVersion` si `--fixed`, sinon
  `^x.y`) — un paquet de plateforme (`php`, `ext-*`) n'est pas dans le
  lock et garde `*` — réécrit `composer.json`, et `Locker::updateHash`
  relit le lock, remplace `content-hash` en place, ajoute les
  `stability-flags` devinés (jamais n'en retire), `fixupJsonDataType`, et
  réécrit avec l'indentation détectée à la lecture, seulement si les
  octets changent et seulement si `doUpdate` a rendu 0. Refus : requérir la
  racine, contrainte invalide, clé incohérente (`require` ↔ `require-dev`)
  → en non-interactif, avertissement et on continue (le paquet est déplacé).
  Édition : `JsonManipulator::addLink` (+ tri), `removeSubNode` de l'autre
  clé, `removeMainKeyIfEmpty` ; repli `JsonFile::write` si le manipulateur
  échoue. Puis `Installer` en update avec liste = paquets requis (sauf
  premier `require` d'un manifeste vide, ou sans lock), mode transitif
  selon `-w`/`-W`, `install` sauf `--no-install`. Échec → `composer.json`
  (et lock) restaurés.
- **`remove`** (`RemoveCommand`) : noms en minuscules remappés vers
  l'orthographe du manifeste, motifs `*` autorisés, paquet présent dans
  l'autre clé → avertissement (pas de déplacement en non-interactif),
  `--unused` (paquets non-dev du lock que rien ne requiert),
  `allow-plugins` nettoyé pour les plugins retirés **après** le retour
  `--no-update` et avant l'update (`removeConfigSetting` → `JsonManipulator`
  ré-sérialise tout le bloc `config`), puis update partielle mode
  `…NO_ROOT_REQUIRE` par défaut (`-W` ou `--no-update-with-dependencies` ;
  `COMPOSER_WITH_DEPENDENCIES` est ignoré par `remove`), même quand le
  paquet n'était qu'une dépendance transitive (il peut alors être mis à
  niveau), restauration de `composer.json` seul en cas d'échec, erreur 2
  si le paquet est encore dans `installed.json` (jamais avec `--no-install`).
- **`VersionSelector`** : candidats de `RepositorySet::findPackages` (toutes
  stabilités si demandé), tri stabilité préférée puis `version_compare`
  décroissant, filtre des exigences de plateforme (php, ext-*) contre le
  `PlatformRepository` local, alias `9999999-dev` → paquet aliasé ;
  `transformVersion` : `x.y.z.w` → `^x.y` (`^0.y.z` sous 1.0), `@stability`
  si non stable, branches dev → alias de branche → `^x.y`, sinon version
  jolie.
- **`JsonManipulator`** : manipulation textuelle par expressions
  régulières. Périmètre réellement atteint par `require`/`remove` :
  constructeur (`trim` + un seul `\n` final, `\r\n` détecté,
  `detectIndenting` = première ligne `^[ \t]+"`), `addLink` (existant :
  regex insensible à la casse, orthographe existante conservée, contrainte
  insérée brute ; section absente → `addMainKey` en fin de fichier ;
  section vide `{}` → construite ; `sortPackages` → `strnatcmp` sensible à
  la casse et reformatage complet de la section par `format`),
  `removeSubNode` (`isset` sensible puis regex insensible ; repli
  `JsonFile::write` sur `PREG_BACKTRACK_LIMIT_ERROR`), `addSubNode`,
  `removeMainKey`, `removeMainKeyIfEmpty`, `removeConfigSetting`, `format`.
  Le reste (`repositories`, listes, `addProperty`) n'est pas porté. Fichier
  absent ou vide → `{\n}\n`.

## 3. Plan

| jalon | livrable | oracle |
|---|---|---|
| P1 | **fait** : `vivace update [pkg…] [-w|-W]` (liste, modes, avertissements, refus de `lock`/`mirrors`/contraintes en ligne) ; c'est aussi la première preuve du dump des paquets d'origine « lock » (`Origin::Locked`), jamais exercé en 0.4 | `tools/oracle-pool.php --update` (5 cas, décisions identiques) ; `harness/update.sh` 7 cas partiels (lock identique) ; à ajouter : un cas sur un nom remplacé, un sur `solver-aliases` (alias racine recréés par `getLockedRepository`) |
| P2 | **fait** : `json_manipulator.rs`, le périmètre ci-dessus avec les motifs de Composer compilés par pcre2 (`s`/`x`/`i`, jamais `u`), `strnatcmp` porté de php-src, sémantique PHP des tableaux décodés (`isset`/`empty`/`array_key_exists`, clés numériques, `array_is_list`, `ArrayObject` = sentinelle `stdClass` de `phpjson`). Revue indépendante : 12 points, 7 divergences confirmées contre le phar et corrigées (section `require` en liste, clés entières → `TypeError` au tri, sous-clé sur chaîne/`false`/`null`, littéral `-0`, U+2028/2029 non échappés par l'encodeur commun — corrigé dans `phpjson`, donc aussi pour le lock —, clés commençant par NUL en décodage objet, profondeur 129–512), plus le code d'erreur pcre2 distingué (`MATCHLIMIT` seul suit le `catch`). Écarts résiduels documentés en tête du module : seuil de backtracking, `1e999`, substitut isolé, > 512 niveaux. `version::preg_quote` corrigé (n'échappe plus `/`, `\0` → `\000`, comme PHP sans délimiteur) | `tests/oracle_json_manipulator.rs` + `tools/oracle-json-manipulator.php` : 794 manifestes réels (fixtures + paquets de fixtures/work) × ~15 scénarios de `require`/`remove` = 12 102 scénarios, + 725 scénarios synthétiques (vide, `{}`, sans `\n` final, `\r\n`, tabulations, 2 espaces, une ligne, sections vides/`null`/chaîne/liste, `\/`, unicode, `#` et espace dans une clé, doublon, nombres, clés entières, sous-clés scalaires, `-0`, U+2028, clé NUL, 200 niveaux, `[]` et BOM refusés) — retours, texte et échecs identiques ; l'oracle attrape une mutation de l'ordre de tri |
| P3 | **fait** : `vivace remove` (`run_remove`) + `config_source.rs` (port de `JsonConfigSource` : manipulateur, repli `JsonFile::write` avec indentation détectée et `{}` pour les tableaux vides du schéma ; validation `LAX_SCHEMA` non portée) ; `merged_allow_plugins` corrigé au passage (ordre `array_merge($projet, $global, $projet)` — la première règle qui correspond décide, l'ordre global-d'abord de 0.4 pouvait inverser un verdict). Refus : `--dry-run`, `--minimal-changes`, `COMPOSER=autre.json`. Revue indépendante : 11 points ; corrigés — code retour 2 sur ensemble insoluble (`Installer::run`, `update` aussi) et restauration seulement sur statut (une exception remonte sans restaurer), repli `composer install` en sous-processus dans `remove` (la suite tournait jamais après `exec`), dépôt local = `installed.json` moins les chemins absents (`purgePackages`), `isLocked` = lock avec clé `packages`, env `COMPOSER_IGNORE_PLATFORM_REQ(S)` (`update` aussi), options acceptées (`-n`, `--no-blocking`, `-q`, `-v`…) ; documentés — schéma LAX, invite interactive, code retour d'une erreur de transport, alias en double dans `--unused`. Constat : `remove -W` sur un motif large (`doctrine/*`) sort de l'instantané (métadonnées jamais capturées : vieilles versions d'orm) → Composer et vivace échouent pareil, codes 100 vs 1 ; à corriger côté capture (rejouer les STEPS dans `snapshot-packagist.sh`) quand un instantané sera refait | `harness/steps.sh` : 33 cas (5 fixtures ; sans lock, lock sans `packages`, paquet encore installé → 2 (avec et sans répertoire), `--unused` positif, ensemble insoluble → 2 + restauration, `allow-plugins` global, `-W`, `--no-update-with-dependencies`, `--no-update`, casse, motifs, `--dev`, paquet dans l'autre section, inexistant, `--unused`, plugins → nettoyage partiel et total d'`allow-plugins`) : composer.json, composer.lock et code retour identiques ; en CI |
| P4 | **fait** : `version_selector.rs` (`find_best_candidate` : tri stabilité préférée puis version — un ordre total, donc indépendant de l'algorithme de tri —, filtre de plateforme avec avertissements `Cannot use …` et leur verbosité, alias `9999999-dev` → base ; `find_recommended_require_version` : règle `ext-*` sur le PHP réel, `transformVersion`, alias de branche) ; `UpdateSession::find_packages_for_require` (composite plateforme + dépôts, `RepositorySet` réduit à `minimum-stability`, `ALLOW_UNACCEPTABLE_STABILITIES`) et `php_version` (PHP sondé, pas `config.platform`). Constat : avec un alias de branche, le candidat retenu est l'alias (`1.x-dev`) et la recommandation sa version jolie ; la branche `^x.y@dev` de Composer n'est atteinte que sans alias | `tests/oracle_version_selector.rs` + `tools/oracle-version-selector.php` : 902 noms sur les 5 instantanés (tous les paquets + `php`, `php-64bit`, `ext-json`, `ext-mbstring`, `ext-nope`, `lib-icu`, `composer-*-api`, une casse mixte) — choix, versions, recommandation, avertissements, `ALLOW_UNACCEPTABLE_STABILITIES` (nul quand `~dev` manque à l'instantané) identiques ; variantes `--ignore-platform-reqs` (laravel), `--ignore-platform-req=php,ext-*,lib-icu+` (sylius, 11 avertissements), et `minimum-stability` dev/beta × `prefer-stable` sur rector (84 noms avec `~dev`, 22 `dev-*`, 47 alias `x.y-dev`). Non exercé par le corpus : `@beta`/`@RC` (test unitaire de `transform_version` seulement) |
| P5 | **fait** : `crates/vivace/src/require.rs` — `parseNameVersionPairs` (dans `version.rs`), `determineRequirements` hors interactif (devinette par `find_best_version_and_name` avec les quatre appels de `findBestCandidate` et les messages d'erreur de Composer, sans `getProviders` ni `findSimilar`), `--fixed` et son contrôle de `type`, clés incohérentes → avertissement, `updateFile` (manipulateur puis repli `JsonFile::write`), `firstRequire`, update partielle en `ONLY_LISTED` par défaut, restauration sur statut ≠ 0 ou exception avant la fin de la résolution, `updateRequirementsAfterResolution` (paquet du lock chargé par l'ArrayLoader, `Locker::updateHash` : content-hash du nouveau manifeste, drapeaux `extractStabilityFlags`, `fixupJsonDataType`, réencodage du lock entier). Refus : `--dry-run`, `--minimal-changes`, `COMPOSER=autre.json`, `config.lock: false` sans `--no-install` (Composer installe depuis son lock virtuel ; vivace n'installe que depuis le fichier — avec `--no-install`, la contrainte devinée vient bien du lock virtuel). Revue indépendante : 16 points ; corrigés — composer.json vide = JSON invalide (erreur, rien d'écrit), lock virtuel pour la devinette, indentation du lock existant conservée (`update` aussi), lock illisible → restauration + erreur, restauration seulement sur l'échec de la résolution (pas de l'installation), `config.lock`/`update-with-minimal-changes` lus dans la config fusionnée avec les valeurs fausses de PHP, `type: null`, lock vide ≠ sauvegarde, `packages: null` ≠ verrouillé, description des surcharges de plateforme (« , actual: x » / « , same as actual ») ; documentés — `getProviders`, code 100 des erreurs de transport, mtime du lock, ordre des avertissements `COMPOSER_IGNORE_PLATFORM_*`. Un point réfuté : `RepositorySet::findPackages` ne s'arrête pas au premier dépôt ici, `require` n'a qu'un `CompositeRepository` dans son set. Constat : un nom dont la devinette ou l'update sort de l'instantané fait échouer Composer en transport (100) — les cas du harness sont choisis dans l'instantané | `harness/steps.sh` : 42 cas `require` (devinette, contrainte explicite en `:`/`=`/deux arguments, `--dev`, `--fixed`, `--no-update`, `-W`/`-w`, `--sort-packages`, sans lock, lock sans `packages`, deux paquets, paquet déjà requis, clé incohérente, `--fixed` sans `type`, ensemble insoluble → 2 + restauration, manifeste vide/absent, `config.lock: false`, lock à 2 espaces, lock illisible, `sort-packages` global, `ext-json` → `*`, extension absente avec et sans `--ignore-platform-req`, `config.platform.php` incompatible, doublons dans les deux ordres, casse, `minimum-stability: dev` sur rector → `8.2.x-dev` et `^4.1@dev` avec `stability-flags`) : composer.json, composer.lock et code retour identiques |
| P6 | docs, CHANGELOG, HANDOVER (miroirs incohérents, `--with`, filtre de sécurité), 0.5.0 | CI verte |
| P7 | `SecurityAdvisoryPoolFilter` : avis capturés dans l'instantané, filtre porté, `--no-blocking` / `COMPOSER_NO_BLOCKING` | oracle du pool avec avis |

Fixtures à étapes (`fixtures/projects/steps-*/`) : `composer.json` +
`composer.lock` de départ (petits, pour garder les instantanés légers) et
`STEPS` (une commande par ligne : `require x`, `require x:^2`, `require
--dev x`, `remove x`, `update x -w`). La capture rejoue les étapes avec
Composer sur Packagist, cache vide, `--no-install --no-scripts
--no-plugins --no-audit` et `COMPOSER_NO_BLOCKING=1`, garde tous les p2
touchés, puis **rejoue toutes les étapes sur l'instantané** et exige
l'égalité étape par étape avant d'archiver `expected/<n>/` (une
publication entre deux étapes rendrait l'archive incohérente). Le harness
`steps.sh` fait tourner Composer et vivace côte à côte sur l'instantané en
CI (même PHP, même plateforme : `findBestCandidate` et le solveur filtrent
sur `php`/`ext-*` locaux) et compare `composer.json` et `composer.lock`
après chaque étape ; `expected/` sert de garde de déterminisme. Cas à
couvrir : nom en casse différente, paquet de plateforme, paquet
transitif retiré, `allow-plugins` nettoyé, manifeste sans
`sort-packages`, manifeste vide, `--dev` déplaçant un paquet.

## 4. Risques
- **`JsonManipulator` est le port le plus exposé** : sa fidélité se juge
  au caractère près sur des entrées variées ; un corpus réel (les
  `composer.json` des fixtures et des paquets des instantanés, qui sont des
  manifestes de bibliothèques) est indispensable.
- **`guess` → deux résolutions** : la seconde passe (`updateHash`) touche
  le lock existant sans le régénérer ; la moindre différence de
  ré-encodage se verrait — l'oracle à étapes la couvre.
- **Verrouillage partiel** : le port de `unlockPackage`/`skippedLoad` n'a
  jamais tourné ; c'est P1, avant tout le reste, avec l'oracle des
  décisions.
- **Effets de bord de Composer non reproduits** : les avertissements
  (« Pattern … does not match any locked packages », « Dependency … is
  also a root requirement ») sont du texte sur stderr ; ils sont portés
  mais pas comparés.
